name: Deploy to Dev

on:
  push:
    branches:
      - main

jobs:
  deploy_everything:
    name: Deploy to Dev Server
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout Repo
        uses: actions/checkout@v3

      - name: 🚀 Deploy via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 34.131.250.104
          username: naman
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd staging/next-app
            git checkout main
            git pull origin main

            echo "📦 Installing dependencies..."
            npm install

            echo "⚙️ Building all apps..."
            cd packages/prisma && npm run build && cd ../..
            cd apps/web && npm run build && cd ../..
            cd apps/http-server && npm run build && cd ../..
            cd apps/ws-server && npm run build && cd ../..

            echo "🔁 Restarting apps using PM2..."
            pm2 restart staging-http-server
            pm2 restart staging-ws-server
            pm2 restart staging-next-app

            echo "✅ Deployment complete!"
